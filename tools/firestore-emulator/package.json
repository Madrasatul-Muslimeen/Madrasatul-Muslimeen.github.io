{
  "name": "quranrevival-firestore-emulator-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "firebase emulators:exec --only firestore --project demo-quranrevival-note-foundation --config firebase.json \"node --test smoke.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.0"
  }
}
